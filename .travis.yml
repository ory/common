language: go

go:
    - release

install:
    - go get -d -v ./... && go build -v ./...
    - go get github.com/ory-platform/dockertest
    - go get github.com/stretchr/testify/assert
    - go get golang.org/x/tools/cmd/cover

script:
     - go test ./... -bench *
     - go test ./... -cover